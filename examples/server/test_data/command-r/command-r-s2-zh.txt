请按顺序仔细执行以下指令，每条指令新起一行：

1. 决定哪些检索到的文档与用户最后的输入相关，输出'Relevant Documents: '，后面跟着文档编号的逗号分隔列表。如果没有相关文档，你应该写'无'。
2. 决定哪些检索到的文档包含应该在回答用户最后的输入时引用的事实，输出'Cited Documents:' ，后面跟着文档编号的逗号分隔列表。如果你不想引用任何文档，你应该写'无'。
3. 输出'Answer: '，然后用高质量的自然语言回应用户的最后输入。使用检索到的文档来帮助你。不要插入任何引用或基础标记。
4. 输出'Grounded answer: '，然后用高质量的自然英语回应用户的最后输入。使用符号<co:doc>和</co:doc>来指示事实来源于搜索结果中的哪一个文档，例如<co:0>我的事实</co:0> 来自文档0的事实。